# Implement-Lower-Bound
Given a sorted array arr[] (0-based) and a target, find the lower bound—the smallest index where the element is ≥ target. If all elements are smaller than the target, return the array’s length.
